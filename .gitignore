# Dependencias
node_modules/

# Secretos y entorno local
.env
.env.*
!.env.example

# Sesión WhatsApp (no subir nunca)
.wwebjs_auth/
.wwebjs_cache/

# QR temporal para vincular (generado en el servidor)
last-qr.png

# Sistema / IDE
.DS_Store
*.log
npm-debug.log*
